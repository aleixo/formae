/// <reference types="react" />
declare function ActionAreaCard({ title, description, onClick, }: {
    title: string;
    description: string;
    onClick(): void;
}): JSX.Element;
export { ActionAreaCard };

{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "dev": "npx -y lerna run dev --scope client-example",
    "build": "npx lerna run build",
    "graph": "npx nx graph"
  },
  "dependencies": {
    "@form-builder/web-components": "^0.0.1",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/node": "18.15.11",
    "@types/react": "18.0.31",
    "@types/react-dom": "18.0.11",
    "lerna": "^6.6.1",
    "typescript": "5.0.3"
  }
}
